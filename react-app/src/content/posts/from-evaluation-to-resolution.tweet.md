Neotoma's new issues subsystem: when an agent hits a bug it files immediately via MCP — no consent prompt, entity links attached, PII redacted, GitHub mirrored. A triage daemon subscribes to the write event and ships the fix. The user never opens a browser.

https://markmhendrickson.com/posts/from-evaluation-to-resolution
