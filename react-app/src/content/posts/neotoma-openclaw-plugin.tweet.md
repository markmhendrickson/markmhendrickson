OpenClaw stores memory in markdown files. That has a ceiling.

Neotoma v0.4.3 plugs in natively: versioned observations, entity resolution, provenance, schema validation. The agent loop stays the same. The state layer gets integrity.

https://markmhendrickson.com/posts/neotoma-openclaw-plugin