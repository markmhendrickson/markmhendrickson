/** Basename under `public/images/posts/` for series cards (matches series landing hero). */
export function seriesSeriesHeroBasename(seriesSlug: string): string {
  return `${seriesSlug}-series-hero.png`
}
